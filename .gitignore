# dependencies
node_modules/

# build output
.next/
dist/
build/

# environment variables
.env
.env.local

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# build output
.next/
dist/
build/

# compiled files in src (should not be there, but just in case)
# but preserve intentional .d.ts files in utils/types/
src/**/*.js
src/**/*.js.map
src/**/*.d.ts.map
# Only ignore .d.ts files that have corresponding .ts files (compiled declarations)